# Maintainer Tools

This folder contains maintainer-only workflows.

- `release.sh`: create an annotated git tag for a release

These scripts are not required for first-time setup, runtime, or normal self-hosted use.
